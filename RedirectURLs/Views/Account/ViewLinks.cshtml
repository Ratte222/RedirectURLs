@model IEnumerable<RedirectURLs.Models.Link>
@{
    ViewBag.Title = "Все пользователи";
}
<a asp-action="Create">Добавить ссылку</a>
<table class="table">
    <tr><td>Короткая ссылка</td><td>Возраст</td></tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.ShortLink</td>
            <td>
                <a asp-action="Details" asp-controller="Account" asp-route-id="@item.Id">Подробнее</a> |
                <a asp-action="Edit" asp-controller="Account" asp-route-id="@item.Id">Изменить</a> |
                <a asp-action="Delete" asp-controller="Account" asp-route-id="@item.Id">Удалить</a>
            </td>
        </tr>
    }
</table>